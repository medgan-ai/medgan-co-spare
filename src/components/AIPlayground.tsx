import React from "react";

// AIPlayground component has been removed
// This component now returns null, effectively removing it from the UI

const AIPlayground: React.FC = () => {
  return null;
};

export default AIPlayground;